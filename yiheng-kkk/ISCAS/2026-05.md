# 2026 年 05 月进展 - 实习生

实习生进展月度汇总

## 黄奕珩 (yiheng-kkk / Benji-code-beeo)

### mentor (Optional)

张洪滨

### 本月工作总结

本月主要围绕 `ruyi-serving` 的模型上下文处理、运行时配置校验和生产可观测性补强开展工作。重点完成了 `meta-llama/Llama-3.1-8B-Instruct` 的多轮上下文 prompt 支持并合入 Upstream，同时提交了运行时配置校验、systemd 配置校验、ready 诊断和 smoke 示例可观测性等一组小改动 PR；月底根据 review 反馈重新整理 issue #24 的默认 `max_tokens` 封顶修复和服务器验证记录。

### 本月提交的PR

- [RuyiAI-Stack/ruyi-serving#16: Validate model serving config fields](https://github.com/RuyiAI-Stack/ruyi-serving/pull/16) - 未合入，已关闭；提交模型 serving 配置字段校验，并在后续整理中加入默认 `max_tokens` 超过模型上限时自动封顶的 issue #24 修复与非流式/流式回归测试。该 PR 后因避免混淆被关闭，issue #24 修复在 6 月单独重提。
- [RuyiAI-Stack/ruyi-serving#18: Validate systemd layout port range](https://github.com/RuyiAI-Stack/ruyi-serving/pull/18) - 未合入，已关闭；提交 systemd 布局端口范围校验、模型 registry id 校验、模型 schema policy enum 校验、readiness failure 诊断，以及 smoke 示例 request id 展示。该 PR 后续按 mentor 要求关闭整理。

### 本月合并的PR

- [RuyiAI-Stack/ruyi-serving#22: Support llama3 chat context prompts](https://github.com/RuyiAI-Stack/ruyi-serving/pull/22) - 为配置 `prompt_style: buddy_meta_llama3` 的模型增加上下文感知 chat prompt 渲染，复用 DeepSeek 的有界上下文选择流程，并实现 Llama 3 chat template 格式；补充 `buddy_meta_llama3` 相关 gRPC contract 测试，并在 wafer 服务器使用真实 `buddy-cli` 与 Llama 3.1 模型路径完成验证。

### 其他交付物

- [RuyiAI-Stack/ruyi-serving#24: api_server 400 Bad Request](https://github.com/RuyiAI-Stack/ruyi-serving/issues/24) - 定位 `default_max_tokens=512`、`max_max_tokens=256` 时未显式传入 `max_tokens` 会被 API Server 误判为超限并返回 400 的问题，整理修复方案和回归测试策略；后续在独立 PR 中补齐非流式与流式场景测试并关联该 issue。
- 完成 `deepseek` 上下文处理流程梳理与 `meta-llama3` 支持方案文档，结合实际运行结果修正文档中的 prompt 构造、上下文选择和 `prompt_style` 识别描述。
- 在 wafer 服务器验证 `meta-llama/Llama-3.1-8B-Instruct` 上下文处理改动：使用 `/wafer/lja/buddy-mlir/build-tenstorrent/bin/buddy-cli`、`llama31_tt.rax` 和 HuggingFace Llama 3.1 模型路径进行真实模型 smoke，确认返回正常。
- 整理 PR #16、PR #18 的 rebase 和冲突处理记录，分别确认远端分支在当时基于最新 `main` 可合并，并在 `buddy-ascend` 服务器运行相关 Python 测试与 pre-commit。
